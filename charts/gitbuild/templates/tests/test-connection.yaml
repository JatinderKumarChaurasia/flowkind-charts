apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gitbuild.fullname" . }}-test-connection"
  labels:
    {{- include "gitbuild.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gitbuild.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
